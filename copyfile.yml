---
- name: copy a file
  hosts: all
  become: true
  tasks:
    - ansible.builtin.copy:
       -  src: /home/ansadmin/ansible_learning/inddex.html
          dest: /home/ansadmin
          mode: 0600
          owner: john

